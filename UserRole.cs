namespace OnlineShoppingSystem;

/// <summary>
/// Defines the roles that control which console menu a signed-in user can access.
/// </summary>
public enum UserRole
{
    Customer = 1,
    Administrator = 2
}
